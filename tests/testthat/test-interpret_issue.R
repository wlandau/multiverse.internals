test_that("interpret_issue() with no problems", {
  expect_equal(
    interpret_issue("abc"),
    "Package abc has no recorded issues."
  )
})

test_that("interpret_issue() with no problems", {
  expect_equal(
    interpret_issue("abc"),
    "Package abc has no recorded issues."
  )
})
