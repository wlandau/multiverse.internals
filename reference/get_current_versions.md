# Get the current versions of packages

Get the current versions of packages in the repo.

## Usage

``` r
get_current_versions(repo = "https://community.r-multiverse.org")
```

## Arguments

- repo:

  URL of the repository to query.

## Value

A data frame of packages with their current versions and hashes.
