# Check for a release.

Check for a release.

## Usage

``` r
assert_release_exists(url)
```

## Arguments

- url:

  Either a character string with the package URL or a custom JSON string
  with a package entry.

## Value

A character string if there is a problem with the package entry,
otherwise `NULL` if there are no issues.
